<%- content_for :action_bar do %>
  <ol>
    <li class="action_bar-item">
      <%= link_to t(".actions.new"), new_company_official_stamp_usage_path, class: "btn text-primary" %>
    </li>
  </ol>
<% end %>

<div class="row justify-content-center">
  <div class="col-lg-12">
    <h3 class="page-title">
      <%= t(".title") %>
    </h3>
    <div class="row">
      <div class="col-lg-12" data-controller="official-stamp-usages-lists">
        <table id="official-stamp-usages-lists-datatable"
          class="table table-hover table-bordered table-responsive"
          data-source="<%= company_official_stamp_usages_path(format: :json) %>">
          <thead>
            <tr>
              <th><%= t(".table.employee_name") -%></th>
              <th><%= t(".table.stamp_to_place") -%></th>
              <th><%= t(".table.stamp_comment") -%></th>
              <th><%= t(".table.attachments") -%></th>
              <th><%= t(".table.created_at") -%></th>
              <th><%= t(".table.task_id_and_status") -%></th>
              <th><%= t(".table.belong_company_department") -%></th>
              <th><%= t(".table.application_class") -%></th>
              <th></th>
            </tr>
          </thead>
          <tbody>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>

<div id="company-official-stamp-usage-modal" class="modal" tabindex="-1" role="dialog"></div>
