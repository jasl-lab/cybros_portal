<%- content_for :action_bar do %>
  <ol>
    <li class="action_bar-item">
      <%= link_to t('.actions.new'), new_company_it_knowledge_maintain_path, class: 'btn text-primary' %>
    </li>
  </ol>
<% end %>

<div class="row justify-content-center">
  <div class="col-lg-12">
    <div class="container">
      <h3 class="page-title">
        <%= t(".title") %>
      </h3>
      <div class="table-responsive">
        <table class="table table-striped">
          <thead>
            <tr>
              <th><%= t('.table.application') -%></th>
              <th><%= t('.table.category') -%></th>
              <th><%= t('.table.question') -%></th>
              <th></th>
            </tr>
          </thead>

          <tbody>
            <% @it_knowledges.each do |it_knowledge| %>
              <tr>
                <td><%= it_knowledge.application %></td>
                <td><%= it_knowledge.category %></td>
                <td><%= it_knowledge.question %></td>
                <td>
                  <%= link_to 'Edit', edit_company_it_knowledge_maintain_path(it_knowledge) %>
                  <%= link_to 'Destroy', company_it_knowledge_maintain_path(it_knowledge), method: :delete, data: { confirm: 'Are you sure?' } %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
