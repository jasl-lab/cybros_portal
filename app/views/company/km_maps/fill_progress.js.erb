var selectize_ctl = $("#select-km-project-progress")[0].selectize;
selectize_ctl.clear();
selectize_ctl.clearOptions(true);
<% @project_progress.each do |u| %>
  selectize_ctl.addOption({text: '<%= u -%>', value: '<%= u -%>'});
<% end %>
