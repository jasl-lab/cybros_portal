var selectize_ctl = $("#select-km-department")[0].selectize;
selectize_ctl.clear();
selectize_ctl.clearOptions(true);
<% @available_departments.each do |u| %>
  selectize_ctl.addOption({text: '<%= u -%>', value: '<%= u -%>'});
<% end %>
