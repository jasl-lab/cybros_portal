var selectize_ctl = $("#select-km-prj-category")[0].selectize;
selectize_ctl.clear();
selectize_ctl.clearOptions(true);
<% @prj_categories.each do |u| %>
  selectize_ctl.addOption({text: '<%= u -%>', value: '<%= u -%>'});
<% end %>
