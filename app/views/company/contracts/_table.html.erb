<div class="table-responsive">
  <table class="table table-striped">
    <thead>
    <tr>
      <th><%= t(".table.contract_id") %></th>
      <th><%= t(".table.contract_name") %></th>
      <th><%= t(".table.amount_total") %></th>
    </tr>
    </thead>
    <tbody>
    <% sas.each do |s| %>
      <tr>
        <td><%= link_to s.salescontractcode, company_contract_sales_contract_path(id: s.salescontractid, contract_id: s.projectcode), remote: true %></td>
        <td><%= s.salescontractshort %></td>
        <td><%= s.amounttotal %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>
