<%= form_with model: rule, url: cost_split_set_baseline_job_type_path(id: rule.id), class: 'form-inline' do |f| %>
  <div class="form-group">
    <%= f.select :user_cost_type_id, SplitCost::UserCostType.pluck(:name, :id), {}, class: 'form-control form-control-sm',onchange: 'Rails.fire(this.form, "submit")' %>
  </div>
  <div class="form-group ml-2">
    <%= link_to cost_split_set_baseline_job_type_path(id: rule.id), remote: true do -%>
      <i class="fas fa-window-close" data-toggle="tooltip" data-placement="top" title="<%= t('.action.cancel') -%>"></i>
    <% end %>
  </div>
<% end %>
