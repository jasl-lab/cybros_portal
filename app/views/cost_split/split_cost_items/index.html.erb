<div class="row">
  <div class="col-lg-12">
    <h3 class="page-title">
      <%= t(".title") %>
    </h3>
    <%= form_tag cost_split_split_cost_items_path, method: :get, class: 'form-inline' do -%>
      <div class="form-group mr-3">
        <label class="mr-1"><%= t(".table.split_cost_item_no") -%>：</label>
        <%= text_field_tag :split_cost_item_no, @split_cost_item_no, class: 'form-control' -%>
      </div>
      <div><%= submit_tag t(".search"), class: 'btn btn-primary' %></div>
    <% end -%>

    <div class="row mt-2">
      <div class="col-lg-12">
        <table class="table table-bordered table-responsive">
          <thead>
            <tr>
              <th><%= t(".table.split_cost_item_no") -%></th>
              <th><%= t(".table.split_cost_item_name") -%></th>
              <th><%= t(".table.split_cost_item_category") -%></th>
              <th><%= t(".table.version") -%></th>
              <th></th>
            </tr>
          </thead>
          <tbody>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
