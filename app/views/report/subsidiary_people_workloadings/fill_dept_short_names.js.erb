var selectize_ctl = $("#select-people-workloading-dept-code")[0].selectize;
selectize_ctl.clear();
selectize_ctl.clearOptions(true);
<% @dept_short_names.each do |d| %>
  selectize_ctl.addOption({text: '<%= d.first -%>', value: '<%= d.second -%>'});
<% end %>
